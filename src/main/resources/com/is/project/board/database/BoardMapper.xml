<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace = "com.is.project.board.database.BoardMapper">

 <select id = "loadBoard">
 
</select>

<insert id="writeBoard" parameterType = "com.is.project.board.model.Board">
	INSERT INTO
	BOARD_TBL(
	TITLE
	,CONTENT
	,USER_ID)
	VALUES(
	#{title}
	,#{content}
	,#{userId}
	
	)
</insert>
</mapper>
